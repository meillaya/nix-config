# Cause Disappearance Ledger — Converged

| cause_id | Initially suspected cause | Disconfirming evidence | Replacement explanation | Current status | Violation no longer observed? |
|---|---|---|---|---|---|
| CD-01 | Missing Wi-Fi SSID/profile explains seeing only `lo` and `docker0` | A profile affects association, not netdev enumeration; exact installed NM/wpa stack exists (O115). | Physical controller/driver/firmware/rfkill/kernel diagnosis is required; profile handoff is a separate first-connect issue. | rejected cause; physical-deferred | **No** — missing interface remains unexplained physically. |
| CD-02 | NetworkManager and `networking.wireless` are competing managers | Exact generated units show NetworkManager intentionally controls wpa_supplicant via D-Bus and no second manager (O51). | Intended NixOS backend wiring. | refuted | **Yes** — alleged manager collision absent. |
| CD-03 | Integrated NixOS starts duplicate NixOS and HM Noctalia units | Exact unit tree contains one integrated Noctalia unit (O85/O121). | Standalone HM unit belongs to a separate output. | refuted | **Yes**. |
| CD-04 | Pure `/.config` paths escape Home Manager and write to `/` | Exact HM link generation prefixes runtime HOME (O127). | Embedded ambient path construction remains fragile but target stays under home. | refuted/narrowed | **Yes** for root write; path fragility remains. |
| CD-05 | 100 MiB ESP cannot boot even one generation | Isolated OVMF boot passed; exact image holds two fully unique current payloads (O80/O82–O83). | It is bootable but lacks reliable rollback/update margin. | replaced cause | **Yes** for absolute boot failure; **No** for capacity risk. |
| CD-06 | Literal `/dev/%DISK%` silently selects/wipes a device | Guarded exact `sgdisk` probe exits 4 on absent literal path (O123). | Unsafe case is a human substituting a wrong valid device without manifest binding. | refuted/narrowed | **Yes** for literal; destructive workflow risk remains. |
| CD-07 | Red Disko installTest proves live nixos-anywhere extra-files install fails | Fixture lacks bootstrap hash; test-only isolation boots successfully (O79–O80). | Production-derived VM fixture is not self-contained; real remote choreography remains unverified. | replaced cause | **Yes** for claimed real-path failure; red release gate remains. |
| CD-08 | Current agenix activation puts plaintext secrets in the store/runtime | `age.secrets` is empty (O101/O117); disposable age primitive preserves boundary (O102). | Risk is latent future login-key coupling; current `sync-secrets` failure is functional. | refuted current exposure | **Yes** for current agenix exposure. |
| CD-09 | Missing microcode prevents generic x86 boot | Generic boot does not require updated microcode; exact closure otherwise broad (O111–O113). | Microcode is security/errata freshness debt. | refuted/narrowed | **Yes** for boot-cause claim; P1 debt remains. |
| CD-10 | One unchanged closure can never span diverse hardware | Exact all-hardware/generic closure evidence spans compatibility classes (O111–O112). | A closure can cover a bounded firmware/boot/storage/driver class, not every machine. | refuted absolute | **Yes** for absolute; universality still false. |
| CD-11 | No macOS Kitty/Fira declaration exists | All six configurations evaluate Kitty/Fira policy; HM has native font copy path (O121/O130). | Only native CoreText/rendering evidence is missing. | refuted statically | **Yes** statically; physical proof deferred. |
| CD-12 | All three notification daemons are guaranteed simultaneously active | Exact activators show competing candidates; isolated activation selected Mako (O87/O119). | Singleton owner is ordering-dependent, not necessarily three live processes. | narrowed | **No** — deterministic ownership remains unresolved. |
| CD-13 | Noctalia state override is inherently a defect | Exact upstream policy intentionally merges runtime state (O90). | Stale warnings/ignored declarative intent are defects; mutable state itself is policy. | refuted/narrowed | **Yes** for inherent-defect claim. |
| CD-14 | Duplicate Darwin app projection necessarily doubles store bytes | 111 visible overlaps/two destinations are proven, but Nix store paths deduplicate (O128). | Ownership/LaunchServices ambiguity, not guaranteed byte duplication. | narrowed | **No** — duplicate projection ownership remains. |

No rejected cause is permitted to reappear in synthesis without new independent
evidence.
