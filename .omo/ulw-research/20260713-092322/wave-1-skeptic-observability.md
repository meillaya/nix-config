# Wave 1 — Causal Skeptic (live digest)

- The validator message is a hard-coded expected value, not the observed tuple. It cannot distinguish owner, group, mode, stat failure, or path context.
- Prior claim that target ownership remained wrong is unsupported until actual metadata is captured.

## EXPAND
- LEAD: change diagnostic to emit numeric and named stat tuple in a failing-first test — WHY: next run must identify the actual violated field — ANGLE: observability-first fix.
