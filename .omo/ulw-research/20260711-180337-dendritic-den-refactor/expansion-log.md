# Expansion log

## Wave 0
- Seeded intents I1-I6 and claims C1-C4.
- Open leads: authoritative den model; flake-parts FAQ; mightyiam patterns; migration experience; hlissner organization; current repo architecture; shell behavior; preservation strategy.

## Wave 1 return: regression/history
- Worker completed with zero unchecked leads.
- Closed: test inventory, staged-work boundaries, dynamic overlays, mutable/generated state, baseline commands.

## Wave 1 returns: module/output/Den/comparison
- New repo leads: Niri closure; BSPWM/Niri session intent; HOME impurity; overlay drift; activation idempotence; app-wrapper runtime; channel helpers; input follows.
- New external leads: dendrix; dendritic-unflake; Den CI scope regressions; namespaces; public before/after migration.
- Core migration claim converged independently: authoritative Den and two dendritic references reject a big-bang migration and favor feature-first incremental extraction.

## Wave 1 return: shells
- Closed full shell inventory.
- Open verification leads: Den user-shell battery; Nushell Home Manager semantics; Ghostty/tmux/OMX precedence after migration.

## Wave 1 return: migration experience
- New leads: Den #629; graph/debug pipeline churn; Rosetta comparison.
- Closed: migration duration cannot be generalized; no Den-specific rollback; incremental coexistence is the only converged safety strategy.

## Wave 1 returns: Nushell and hlissner
- Open shell leads: target-lock option verification; Ghostty/tmux integration; POSIX profile audit.
- Open organization leads: typed entity facts, app aspects, explicit secret wiring.
- Closed: hlissner is not an architecture template; only bounded taxonomy ideas transfer.

## Wave 1 returns: Den shell and public adopters
- Critical finding: never call `den.batteries.user-shell "nushell"`; implement explicit OS registration + HM config.
- Open: two-platform target-pin fixture; routing issues #473/#609/#629; strongest current adopters; deprecated fixture semantics.
- Closed: big one-commit migrations are not evidence of safety and will not guide this refactor.

## Wave 1 return: skeptic
- Rejected contaminated local-repo conclusion (wrong cwd).
- Retained source-backed risks: v0 churn, resolution-layer debugging, recent routing issues, conceptual lock-in.
- Open: pinned minimal pilot and #629 status.

## Wave 2 return: migration matrix
- File-to-entity/class/aspect mapping is closed.
- One optional asset-level lead remains; operational payloads can stay in place because architecture migration need not relocate non-Nix assets.

## Wave 2 return: routing
- Closed #473/#609/#629 semantics with executed fixtures.
- Architecture rule: avoid direct host-class user arguments and accidental host-scope HM; user HM belongs on user aspect.

## Wave 2 return: adopters
- Closed public-adopter topology and deprecated compatibility leads.
- Selected pattern: vix entity topology + nixicle output boundary + restrained aggregate aspects.

## Wave 2 return: target-lock pilot
- Closed target-lock option and direct-aspect route.
- Residual: live Darwin login activation cannot be proved from Linux; preserve as explicit residual risk.

## Wave 2 return: shell locks
- Closed locked option inventory and current precedence.
- Deferred post-change Nu runtime/generated-file checks to implementation verification.
- Policy decision candidate: Nushell is primary interactive shell; keep interpreter-specific wrappers/scripts unchanged.

## Wave 2 return: output boundary
- Closed composer decision with executed fixture.
- Flake-parts outer + Den configuration entities + perSystem operational outputs is selected.
- x86_64-darwin lifecycle remains a documented external pin constraint, not a blocker for current lock.
